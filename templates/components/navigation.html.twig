<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('home') }}">Test_Disko</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        {#<ul class="navbar-nav">#}
            {#<li class="nav-item active">#}
                {#<a class="nav-link" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>#}
            {#</li>#}
            {#<li class="nav-item">#}
                {#<a class="nav-link" href="#">Liste des produits</a>#}
            {#</li>#}
        {#</ul>#}
        {#<ul class="navbar-nav mx-auto">#}
            {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
                {#<li><a>Bienvenue {{ app.user.firstname }}</a></li>#}
                {#</a>#}
            {#{% else %}#}
                {#<li><a>Bienvenue</a></li>#}
            {#{% endif %}#}
        {#</ul>#}
        {#<ul class="navbar-nav ml-auto">#}
            {#{% if is_granted('ROLE_ADMIN') %}#}
                {#<li class="nav-item">#}
                    {#<a class="nav-link" href="#">Mon esitpace</a>#}
                {#</li>#}
            {#{% endif %}#}
            {#<li class="nav-item">#}
                {#<div>#}
                    {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
                        {#<a  class="nav-link" href="{{ path('fos_user_security_logout') }}">#}
                            {#{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}#}
                        {#</a>#}
                    {#{% else %}#}
                        {#<a href="{{ path('fos_user_security_login') }}" data-toggle="modal" data-target="#connection" class="nav-link">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>#}
                    {#{% endif %}#}
                {#</div>#}
                {#<a class="nav-link" href="#" data-toggle="modal" data-target="#connection">Connection</a>#}
            {#</li>#}
        {#</ul>#}
        <ul class="navbar-nav ml-auto">
            {% if not app.user %}
                <li class="nav-item">
                    <a  class="nav-link" href="{{ path('security_login') }}">Connection</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a  class="nav-link" href="{{ path('security_logout') }}">Déconnection</a>
                </li>
            {% endif %}
        </ul>



    </div>
</nav>

<!-- Modal -->
<div class="modal fade" id="connection" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modalités du formulaire</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            {#{{ include('forms/modalFormLogin.html.twig', {'form': form}) }}#}
            <div class="modal-footer">
                <button type="button" class="btn btn-primary">Save changes</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>