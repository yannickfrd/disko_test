{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/global.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/produit/new.css') }}">
{% endblock %}

{% block title %}Modifier Produit{% endblock %}

{% block body %}
    <div class="container">
        <div class="jumbotron example-wrapper">
            <h1 class="display-4">Modifier un produit Produit.</h1>
            <hr class="my-4">
            <p class="lead">
                {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
            </p>
            <hr class="my-4">
            <div class="row">
                <a class="btn btn-outline-info" role="button" type="button" href="{{ path('produit_index') }}">Retour à la liste</a>
                {{ include('produit/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
